#include <stdio.h>
int main()
{
int n, d,arr[n];
    printf("Enter the size: ");
    scanf("%d", &n);
   printf("Enter the elements :\n");
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    printf("Enter the number of times: ");
    scanf("%d", &d);
}
